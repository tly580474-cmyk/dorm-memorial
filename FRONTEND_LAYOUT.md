# 宿舍电子纪念社区前端布局规划

> 文档版本：v0.1  
> 日期：2026-08-25  
> 关联文档：[开发计划](./DEVELOPMENT_PLAN.md)  
> 设计基线：[设计系统](./design-system/dorm-memorial-community/MASTER.md)

## 1. 布局目标

前端需要同时承载纪念册、照片墙、论坛和即时通信，但不应呈现为功能堆叠的后台系统。整体体验以“回忆浏览”为中心，交流功能围绕内容自然展开。

- 首页快速呈现最近动态和过去的回忆。
- 图片和视频是主要视觉内容，长文字仍适合阅读。
- 论坛、群聊和私信入口明确，但不压过纪念内容。
- 手机端完成上传、浏览、回复和聊天时无需横向滚动。
- 远端媒体加载慢或不可用时，页面结构保持稳定。
- 所有主要功能支持键盘和触屏访问。

## 2. 信息架构

```text
宿舍纪念社区
├── 首页：往年今日、快速发布、最近动态
├── 时间线：按年月浏览、宿舍事件
├── 照片墙：图片、视频、筛选和标签
├── 留言册：宿舍留言、成员留言
├── 论坛：板块、主题、回复
├── 消息：宿舍群聊、私信
├── 我的：主页、投稿、草稿、喜欢、设置
└── 管理：审核、邀请、存储、备份、审计
```

## 3. 全局页面框架

### 3.1 桌面端（≥1280px）

```text
┌──────────────────────────────────────────────────────────────────────────┐
│ Logo / 宿舍名       全局搜索          发布  通知  头像                   │
├───────────────┬──────────────────────────────────────┬───────────────────┤
│ 首页          │ 页面标题 / 页面工具                   │ 室友状态          │
│ 时间线        │                                      │ 最近话题          │
│ 照片墙        │ 主内容区域                            │ 往年今日          │
│ 留言册        │ 680–860px                            │ 存储状态提示      │
│ 论坛          │                                      │                   │
│ 消息  ●       │                                      │ 仅需要时显示      │
│ 我的          │                                      │                   │
│ 管理          │                                      │                   │
└───────────────┴──────────────────────────────────────┴───────────────────┘
```

- 页面最大宽度 1440px，并在超宽屏居中。
- 左栏固定 224px；中栏自适应；右栏 280px。
- 普通信息流最大 760px，照片墙可扩展到中栏全宽。
- 右栏只在首页、论坛列表和消息列表等有价值的页面显示。
- 顶栏高 64px，吸顶但不覆盖内容。

### 3.2 平板端（768–1279px）

- 左侧导航收窄至 72～80px，只显示图标和悬浮提示。
- 右侧信息栏隐藏，其内容移到首页主内容下方。
- 论坛和消息使用约 280px 列表 + 详情的双栏结构。

### 3.3 移动端（<768px）

```text
┌──────────────────────────┐
│ 宿舍名       搜索  通知   │  56px 顶栏
├──────────────────────────┤
│                          │
│       当前页面内容        │
│                          │
├──────────────────────────┤
│ 首页  照片  ＋  论坛  消息 │  64px 底栏
└──────────────────────────┘
```

固定底栏保留首页、照片墙、发布、论坛和消息五项。时间线、留言册、个人主页、设置和管理放入头像菜单或“更多”底部面板。底栏适配 iOS Safe Area。

## 4. 全局导航与公共组件

### 4.1 顶栏与侧栏

- 顶栏包含宿舍名称、全局搜索、发布、通知和个人菜单。
- 移动端顶栏只保留宿舍名、搜索和通知，发布由底栏中央按钮承担。
- 桌面侧栏顺序为：首页、时间线、照片墙、留言册、论坛、消息、我的。
- 管理入口经分隔线放在最下方，普通成员不可见。

### 4.2 全局发布器

- 桌面端使用最大宽度 760px 的居中对话框或独立编辑页。
- 移动端使用全屏编辑页。
- 第一层只显示正文、添加媒体和发布按钮。
- 日期、标签、提及和可见范围放入“更多设置”。
- 自动保存草稿，上传进度按文件展示，失败文件可以单独重试。

### 4.3 全局搜索

结果按纪念内容、媒体、论坛主题和成员分组。桌面端使用命令面板式浮层，移动端使用全屏搜索页。聊天私信默认不进入全局搜索。

## 5. 核心页面布局

### 5.1 首页

```text
┌──────────────────────────────────────┐
│ 问候语 + 宿舍合照 / 当前纪念主题      │
├──────────────────────────────────────┤
│ 往年今日：小卡片或单张精选             │
├──────────────────────────────────────┤
│ 快速发布：分享一句话 / 图片 / 视频      │
├──────────────────────────────────────┤
│ 最近动态                              │
│ ┌──────────────────────────────────┐ │
│ │ 作者、时间、正文                  │ │
│ │ 媒体网格                          │ │
│ │ 喜欢、评论、更多                  │ │
│ └──────────────────────────────────┘ │
└──────────────────────────────────────┘
```

- 首屏只有一个主要视觉焦点。
- “往年今日”无数据时自动隐藏。
- 动态流第一版使用分页或“加载更多”，不强制无限滚动。
- 每条动态最多预览四张媒体，多余内容显示数量蒙层。

### 5.2 时间线

桌面端左侧使用年月索引，右侧显示事件流；移动端把年月索引改为顶部筛选器。时间轴装饰保持简洁，内容日期与发布日期分开显示，跳转月份后同步更新 URL。

### 5.3 照片墙

```text
┌──────────────────────────────────────────────────┐
│ 照片墙     全部 / 图片 / 视频     成员  日期  标签 │
├──────────────────────────────────────────────────┤
│ ┌────────┐ ┌─────────────┐ ┌────────┐             │
│ │        │ │             │ │        │             │
│ └────────┘ └─────────────┘ └────────┘             │
│ ┌─────────────┐ ┌────────┐ ┌─────────────┐        │
│ │             │ │        │ │             │        │
│ └─────────────┘ └────────┘ └─────────────┘        │
└──────────────────────────────────────────────────┘
```

- 手机两列、平板三列、桌面四至五列。
- 视觉上可用瀑布流，但 DOM 顺序必须符合阅读顺序。
- 桌面筛选栏吸顶，移动端筛选使用底部面板。
- 视频封面显示播放图标和时长。
- 点击进入媒体查看器，浏览器返回操作应关闭查看器。

### 5.4 内容详情

- 桌面正文居中，媒体可适度突破正文宽度。
- 作者、内容日期、发布时间和修改状态清晰分层。
- 评论区位于正文之后，不使用永久右侧抽屉。
- 删除、复制链接等低频操作放入“更多”。
- 移动端评论输入栏不能遮挡最后一条评论。

### 5.5 留言册

留言册首页先显示“给整个宿舍留言”，随后是室友头像列表。个人留言页由资料头、精选留言和单列留言卡组成。允许轻微纸张色差，但不使用随机旋转文字或密集便利贴墙。

### 5.6 论坛

- 首页展示板块简介与最近主题，不突出复杂统计。
- 桌面主题列表显示标题摘要、作者、回复数和最后活动。
- 移动端将元信息折叠到标题下方，禁止横向表格。
- 主题详情以连续楼层展示，引用内容使用克制的左边框。

### 5.7 群聊与私信

桌面端使用会话列表和当前会话双栏：

```text
┌──────────────────┬──────────────────────────────────┐
│ 搜索会话          │ 会话名称、成员、更多             │
│ 宿舍群聊  3       ├──────────────────────────────────┤
│ 小明              │          消息历史                │
│ 小红              ├──────────────────────────────────┤
│                  │ 附件  输入消息              发送  │
└──────────────────┴──────────────────────────────────┘
```

- 会话栏宽 300～340px。
- 消息正文最大宽度约为内容区的 72%。
- 连续同一作者消息合并头像与时间。
- 仅当用户接近底部时自动滚动到新消息。
- 移动端会话列表和聊天详情使用独立路由；聊天详情隐藏全局底栏。

### 5.8 个人主页与通知

- 个人页顶部显示头像、昵称、简介和纪念信息，标签页为“纪念、媒体、留言”。
- 不展示粉丝数、关注数等不符合私密小社区的指标。
- 通知按“今天、较早”分组，统一呈现回复、提及、喜欢、私信和系统通知。

### 5.9 管理后台

- 使用独立 `/admin` 路由和更紧凑的工作台布局。
- 导航包含审核、用户与邀请、存储与任务、备份与导出、审计日志。
- 危险操作二次确认并说明影响范围。
- 桌面可用表格，移动端改为摘要卡片和详情页。

## 6. 路由规划

```text
/
/timeline
/wall
/guestbook
/guestbook/:userId
/posts/new
/posts/:postId
/forum
/forum/boards/:boardId
/forum/threads/:threadId
/messages
/messages/:conversationId
/notifications
/members/:userId
/me/posts
/me/drafts
/settings
/admin/*
```

媒体查看器使用可链接状态，例如 `/wall?media=<mediaId>`，确保刷新、分享和浏览器返回正常。

## 7. 响应式规则

| 断点 | 导航 | 内容列 | 照片墙 | 消息 |
| --- | --- | --- | --- | --- |
| `<640px` | 顶栏 + 底栏 | 单列 | 2 列 | 独立页面 |
| `640–767px` | 顶栏 + 底栏 | 单列 | 2～3 列 | 独立页面 |
| `768–1023px` | 紧凑侧栏 | 主列 | 3 列 | 双栏可选 |
| `1024–1279px` | 紧凑侧栏 | 主列 | 4 列 | 双栏 |
| `≥1280px` | 完整侧栏 | 主列 + 右栏 | 4～5 列 | 双栏 |

在 375、768、1024、1280 和 1440px 进行布局验收。

## 8. 页面状态

每个页面都需要覆盖：

- 首次加载：保留尺寸的骨架屏。
- 空状态：说明原因并给出一个主要行动。
- 远端媒体不可用：保留文字、作者和尺寸占位，提供重试。
- 权限不足：不泄露目标内容是否存在。
- 上传中：单文件及总体进度。
- 上传失败：保留正文草稿并允许单文件重试。
- WebSocket 断开：低干扰连接提示。
- 内容已删除：引用和通知中使用统一删除占位。

## 9. 视觉与交互细节

- 暖白背景、陶土红主色、墨绿色辅助色。
- 中文标题使用宋体风格，正文和界面使用无衬线字体。
- 胶片颗粒仅用于登录页、空状态或大型封面，透明度不超过 4%。
- 不给每张卡片添加胶带、撕纸和随机旋转装饰。
- 卡片依赖边框和留白，阴影只用于浮层与精选内容。
- 动画持续 150～300ms，并尊重减少动态效果设置。
- 点击目标至少 44×44px，相邻目标间隔至少 8px。
- 使用统一 Lucide SVG 图标，不用 Emoji 作为功能图标。

## 10. 前端组件拆分

```text
AppShell
├── DesktopSidebar / TopBar / MobileBottomNav / RightRail
Content
├── PostComposer / PostCard / MediaGrid / MediaViewer
├── CommentThread / ReactionButton / MemberChip
Forum
├── BoardCard / ThreadListItem / ThreadComposer / ForumReply
Messaging
├── ConversationList / MessageList / MessageBubble / MessageComposer
Feedback
└── Skeleton / EmptyState / ErrorState / Toast / UploadProgress
```

组件先保持业务语义，不急于构造过度抽象的通用组件库。

## 11. 第一轮原型范围

1. 桌面端与移动端首页。
2. 桌面端与移动端照片墙。
3. 发布编辑器与上传进度。
4. 论坛列表与主题详情。
5. 桌面消息双栏与移动聊天详情。
6. 个人留言页。

这些页面覆盖导航、信息流、媒体、长文本、表单和实时通信的主要布局问题。管理后台在社区布局验证后单独设计。

## 12. 布局验收标准

- 两次操作内到达照片墙、论坛、群聊和发布页。
- 移动底部导航不超过五项。
- 375px 宽度下无横向滚动。
- 固定顶栏和底栏不遮挡正文、评论和聊天输入框。
- 照片墙加载前后无明显布局跳动。
- 键盘可完成导航、发布、评论和关闭媒体查看器。
- 远端媒体失败时整条帖子仍可阅读。
- 聊天断线重连后准确回到原会话位置。
- 普通成员页面不出现管理员专属操作。
- 浅色与深色模式均满足正文对比度要求。

