# 宿舍电子纪念社区开发计划

> 文档版本：v0.1  
> 编写日期：2026-08-25  
> 当前状态：阶段 2（纪念册核心）进行中；阶段 0、阶段 1 本地开发已完成
> 目标用户：5～6 名宿舍成员  
> 预计媒体总量：不超过 100 GiB

前端信息架构、响应式布局与页面线框参见 [前端布局规划](./FRONTEND_LAYOUT.md)。

## 1. 项目概述

本项目用于构建一个私有的宿舍电子纪念社区。成员可以发布文字、图片和视频，浏览照片墙，在论坛中交流，并通过群聊和私信保持联系。项目应当在毕业或活动结束后支持完整导出，避免内容永久依赖在线服务。

项目不再基于 Immich 二次开发。采用自研轻量应用承载社区功能，以 AList 作为第三方存储适配层，原始媒体保存在夸克网盘，生产主机只保存业务数据、小尺寸衍生文件和有限缓存。

## 2. 已确认的基础条件

### 2.1 用户与数据规模

- 用户数量为 5～6 人。
- 原始图片和视频总量预计不超过 100 GiB。
- 访问量和聊天并发量较低。
- 项目默认是邀请制私有社区，不以公开传播为目标。

### 2.2 生产主机实测情况

| 项目 | 实测结果 |
| --- | --- |
| 操作系统 | Ubuntu 24.04.2 LTS |
| 虚拟化 | KVM |
| CPU | 2 vCPU，Intel Xeon Platinum |
| 内存 | 1.6 GiB |
| Swap | 2 GiB |
| 系统盘 | 40 GiB ext4 |
| 可用磁盘 | 约 24 GiB |
| Web 服务 | Nginx 1.24 |
| 容器环境 | 未安装 Docker |
| 已运行服务 | Nginx、FRPS、New API、阿里云相关服务 |
| 防火墙 | UFW，仅开放 22、80、443 |

### 2.3 主机约束

- 不在生产主机持久保存原始图片和视频。
- 不在生产主机进行完整视频转码。
- 不引入 Redis、消息队列或微服务体系。
- 不依赖 Node.js 作为生产运行时。
- 应用必须限制并发上传、内存缓冲、缓存和日志大小。
- 数据库与缩略图必须有独立备份，不能只存在生产主机。

## 3. 产品目标

### 3.1 核心目标

1. 每位成员拥有独立账号和个人主页。
2. 支持发布文字、图片和视频组成的纪念内容。
3. 支持照片墙、个人作品流和宿舍时间线。
4. 支持留言、评论、回复、点赞和提及。
5. 支持论坛板块、主题帖和楼层回复。
6. 支持宿舍群聊和成员私信。
7. 支持内容审核、软删除和操作记录。
8. 支持完整导出文字、元数据和原始媒体。
9. 在夸克或 AList 临时不可用时，文字内容仍可访问。
10. 存储后端可以在未来从夸克迁移到 S3、WebDAV 或本地存储。

### 3.2 非目标

第一阶段不实现以下能力：

- 手机相册自动备份。
- 人脸识别与聚类。
- AI 图片搜索和内容生成。
- 服务端复杂图片编辑。
- 服务端完整视频转码流水线。
- 音视频通话。
- 大规模公开社区能力。
- 多租户或商业化计费。
- 原生 Android、iOS 客户端。

## 4. 总体架构

```text
成员浏览器
    │
    │ HTTPS / WebSocket
    ▼
Nginx
    │
    ▼
纪念社区 Go 单体应用
    ├── HTTP API
    ├── 静态前端
    ├── WebSocket Hub
    ├── SQLite WAL
    ├── 后台任务
    ├── 本地缩略图缓存
    └── ObjectStorage 接口
               │
               ▼
             AList
               │
               ▼
            夸克网盘
```

### 4.1 部署形态

- Go 应用编译为单一原生可执行文件。
- 前端在开发机或 CI 中构建为静态文件。
- 静态文件嵌入 Go 程序或由 Nginx 提供。
- Go 应用与 AList 均由 systemd 管理。
- AList 仅监听回环地址，不直接暴露公网。
- Nginx 只公开纪念社区需要的 HTTPS 和 WebSocket 路径。
- 生产环境不安装 Docker，减少磁盘与内存开销。

## 5. 技术选型

| 层级 | 选择 | 说明 |
| --- | --- | --- |
| 后端 | Go | 常驻内存低，适合单文件部署与流式 IO |
| HTTP | Go 标准库或轻量路由器 | 避免大型框架依赖 |
| 前端 | Vue 3 或 Svelte | 构建后作为静态资源运行 |
| 数据库 | SQLite WAL | 单机、低并发、易于备份和导出 |
| 数据访问 | SQLC 或轻量 SQL 封装 | 保持 SQL 清晰且可测试 |
| 实时通信 | WebSocket | 群聊、私信、未读提示和在线状态 |
| 文件存储 | AList API/WebDAV | 当前连接夸克，未来可替换 |
| Web 入口 | Nginx | HTTPS、限流、上传反向代理 |
| 进程管理 | systemd | 自动启动、重启和资源限制 |
| 搜索 | SQLite FTS5 | 搜索帖子、留言与论坛内容 |
| 测试 | Go testing + 前端组件测试 + Playwright | 覆盖单元、集成和关键用户流程 |

前端框架在原型开始前最终确定。若两者没有现有代码偏好，默认选择 Vue 3，以降低后续维护门槛。

## 6. 功能模块

### 6.1 用户与身份

- 管理员创建邀请链接或邀请码。
- 用户通过邀请完成注册。
- 用户名、昵称和邮箱保持唯一。
- 支持修改头像、个人介绍、床号和纪念信息。
- 使用服务端 Session Cookie，不将长期认证令牌暴露给前端脚本。
- 支持查看并注销其他登录会话。
- 管理员可以禁用账号，但默认不物理删除其历史内容。

### 6.2 纪念内容

- 发布纯文字、图文或视频内容。
- 支持草稿、待审核、已发布、已隐藏和已删除状态。
- 支持自定义内容日期，以记录过去发生的事件。
- 支持标签、提及成员和可见范围。
- 支持编辑历史或至少记录最后修改时间。
- 支持评论、回复和点赞。

### 6.3 留言册

- 每位成员拥有个人留言页。
- 成员可以给个人或整个宿舍留言。
- 留言可以附带图片和视频。
- 留言接受者和管理员可以隐藏不合适内容。

### 6.4 照片墙

- 使用响应式网格或瀑布流展示图片和视频封面。
- 支持按成员、日期、标签和媒体类型筛选。
- 默认加载缩略图，查看详情时才请求原始媒体。
- 支持点赞、评论、下载和查看所属内容。
- 远端媒体暂时不可用时显示占位状态，不影响页面其他内容。

### 6.5 论坛

- 支持板块、主题帖和回复。
- 支持置顶、锁定、隐藏和软删除。
- 支持引用回复、成员提及和媒体附件。
- 第一版采用时间顺序，不实现复杂推荐算法。

### 6.6 群聊与私信

- 预置一个宿舍群聊。
- 支持一对一私信。
- 支持文字、表情和媒体附件。
- 支持未读计数、已读游标和消息撤回。
- 第一版不提供端到端加密。
- 消息内容存储在应用数据库中，管理员的技术访问边界必须在隐私说明中明确。

### 6.7 通知

- 评论、回复、提及、点赞、私信和审核结果产生站内通知。
- 支持未读数量和批量标记已读。
- 第一版不发送短信或移动推送。
- 邮件通知作为后续可选功能。

### 6.8 管理后台

- 用户和邀请管理。
- 投稿、留言和论坛内容审核。
- 媒体孤立记录与上传失败任务检查。
- 用户配额和总存储用量查看。
- 数据库备份、导出任务和恢复检查。
- 应用版本、AList 状态和远端存储健康状态。
- 审计日志查询。

## 7. 权限模型

初始角色分为 `admin` 和 `member`。

| 操作 | 管理员 | 成员 |
| --- | :---: | :---: |
| 创建邀请 | 是 | 否 |
| 浏览已发布内容 | 是 | 是 |
| 发布内容 | 是 | 是 |
| 编辑自己的内容 | 是 | 是 |
| 编辑他人内容 | 是 | 否 |
| 隐藏或恢复内容 | 是 | 仅自己的内容 |
| 永久删除媒体 | 是 | 否 |
| 创建论坛板块 | 是 | 否 |
| 发布论坛主题与回复 | 是 | 是 |
| 访问群聊 | 是 | 是 |
| 访问自己的私信 | 是 | 是 |
| 查看系统审计日志 | 是 | 否 |
| 执行全量导出 | 是 | 否 |

私信默认只对会话参与者可见。是否允许管理员通过后台读取私信，需要在上线前形成明确且公开的隐私规则。

## 8. 数据模型初稿

### 8.1 核心实体

- `users`：用户、角色和状态。
- `sessions`：登录会话和设备信息。
- `invites`：邀请凭据、使用次数和有效期。
- `profiles`：昵称、头像、简介和宿舍资料。
- `posts`：纪念内容、草稿和发布状态。
- `media`：远端对象路径、哈希、大小和媒体信息。
- `post_media`：内容与媒体的排序关系。
- `comments`：评论和嵌套回复。
- `reactions`：点赞或其他反应。
- `guestbook_entries`：个人或宿舍留言。
- `forum_boards`：论坛板块。
- `forum_threads`：论坛主题。
- `conversations`：群聊和私信会话。
- `conversation_members`：会话成员和已读游标。
- `messages`：聊天消息和撤回状态。
- `message_media`：聊天附件。
- `notifications`：站内通知。
- `tags` 与 `content_tags`：标签系统。
- `upload_jobs`：上传状态和失败清理任务。
- `audit_logs`：关键管理操作记录。
- `app_settings`：受控的应用配置。

### 8.2 数据库原则

- 启用外键约束和 WAL 模式。
- 所有主要记录使用不可预测的 UUID 或同级标识符。
- 所有时间以 UTC 存储，界面按 Asia/Shanghai 展示。
- 删除业务内容优先采用软删除。
- 媒体永久删除只能由受控后台任务执行。
- 数据库迁移必须支持向前升级并在升级前自动备份。

## 9. 存储抽象

业务代码只依赖统一接口，不直接依赖夸克协议：

```go
type ObjectStorage interface {
	Put(ctx context.Context, path string, body io.Reader, size int64) error
	Open(ctx context.Context, path string) (io.ReadCloser, error)
	Stat(ctx context.Context, path string) (ObjectInfo, error)
	Delete(ctx context.Context, path string) error
	Move(ctx context.Context, from, to string) error
	ResolveURL(ctx context.Context, path string) (string, error)
}
```

第一版实现 `AListStorage`。后续可以增加 `S3Storage`、`WebDAVStorage` 和 `LocalStorage`，而不改变内容、论坛或聊天模块。

### 9.1 远端目录建议

```text
/dorm-memorial/
├── originals/
│   └── <user-id>/<year>/<month>/
├── previews/
├── avatars/
├── exports/
└── quarantine/
```

数据库保存稳定的对象路径，不保存可能过期的夸克直链。

## 10. 上传与媒体处理

### 10.1 上传流程

1. 浏览器检查文件类型、大小和用户配额。
2. 浏览器计算 SHA-256，并尽可能生成图片缩略图或视频封面。
3. 应用再次验证身份、配额、扩展名、MIME 和请求大小。
4. 应用使用有限缓冲将文件流式转发到 AList。
5. AList 将文件写入夸克网盘。
6. 应用调用 `Stat` 验证远端文件大小。
7. 应用写入 `media` 和业务关联记录。
8. 数据库写入失败时创建远端孤立文件清理任务。

### 10.2 初始限制

| 项目 | 初始限制 |
| --- | --- |
| 图片单文件 | 50 MiB |
| 视频单文件 | 2 GiB |
| 用户媒体配额 | 20 GiB，可由管理员调整 |
| 同时上传 | 全站最多 2 个大文件任务 |
| 图片类型 | JPEG、PNG、WebP、HEIC |
| 视频类型 | MP4、MOV |
| 在线播放推荐 | MP4 / H.264 / AAC |

### 10.3 生产机缓存原则

- 禁止将完整上传内容一次性读入内存。
- 禁止无界临时文件和无界 WebDAV 缓存。
- 上传接口关闭 Nginx 请求体磁盘缓冲。
- 媒体磁盘缓存默认分配 2 GiB（`APP_MEDIA_CACHE_MAX_BYTES=2147483648`），完整读取后入缓存并按最近访问时间 LRU 淘汰。
- 新上传对象同步写入本地缓存；命中缓存的图片、头像和视频 Range 不再向 AList 请求。
- 视频不兼容时先提供下载，不在第一版强制转码。

## 11. API 与实时协议

### 11.1 HTTP API

API 初步按以下领域划分：

```text
/api/auth/*
/api/users/*
/api/profiles/*
/api/posts/*
/api/media/*
/api/guestbook/*
/api/forum/*
/api/conversations/*
/api/messages/*
/api/notifications/*
/api/admin/*
/api/export/*
```

- 使用 JSON 返回业务数据。
- 媒体上传使用流式请求体或 multipart。
- 所有列表接口使用稳定分页。
- 错误响应使用统一错误码和可读消息。
- 写操作进行 CSRF 防护。
- 幂等上传需要客户端请求 ID。

### 11.2 WebSocket

WebSocket 负责：

- 新聊天消息。
- 消息撤回。
- 已读游标更新。
- 通知提醒。
- 会话级在线状态。

断线重连后以数据库游标补拉消息，不能依赖内存保留唯一消息副本。

## 12. 安全与隐私

### 12.1 访问控制

- 默认关闭公开注册。
- 默认所有内容仅登录成员可见。
- 禁止搜索引擎索引。
- AList 后台及 WebDAV 只监听回环地址或受限网络。
- 夸克使用专用账号，避免与私人网盘数据混用。
- AList Cookie、应用密钥和数据库备份密钥不得进入 Git。

### 12.2 上传安全

- 同时验证扩展名、MIME 和文件头。
- 拒绝可执行文件、HTML、SVG 和脚本文件作为媒体上传。
- 文件名仅用于展示，远端对象名由服务端生成。
- 设置用户配额、请求频率和并发限制。
- 对图片解析库设置像素数量上限，防止解压炸弹。
- 下载响应设置安全的 `Content-Disposition` 和 `Content-Type`。

### 12.3 主机安全

- 保持 UFW 仅开放 22、80、443。
- 核查 SSH 失败登录并保持仅密钥认证。
- 后续考虑禁止 root 直接登录或部署 Fail2ban。
- AList、应用服务均使用独立低权限系统用户。
- systemd 启用 `NoNewPrivileges`、受限写目录和资源限制。
- AList 使用不低于修复已知 TLS 漏洞的版本。

### 12.4 隐私规则待确认项

- 管理员是否可以读取私信内容。
- 用户注销后内容是保留、匿名化还是删除。
- 投稿截止后是否仍允许撤回。
- 是否允许下载他人上传的原始媒体。
- 是否允许成员生成外部分享链接。

## 13. 备份、恢复与导出

### 13.1 备份范围

- SQLite 数据库。
- 应用配置的非敏感部分。
- 用户头像和本地缩略图。
- AList 数据库与存储配置。
- 远端媒体对象清单与 SHA-256。
- 加密后的必要密钥恢复材料。

### 13.2 备份策略

- SQLite 每日在线备份。
- 备份文件加密后上传至远端。
- 至少保留 7 个每日备份和 4 个每周备份。
- 数据库备份不能只放在同一台生产主机。
- 夸克不能作为纪念资料的永久唯一副本。
- 投稿结束后生成一次离线完整副本。
- 每个正式版本至少进行一次恢复演练。

### 13.3 最终导出

项目应支持：

- 静态 HTML 纪念册。
- Markdown 或 JSON 文字与结构化数据。
- 原始媒体目录及校验清单。
- 聊天与论坛内容的可读归档。
- 可选 PDF 精简纪念册。
- 可迁移到其他对象存储的路径映射清单。

## 14. 可观测性与运维

### 14.1 健康检查

- 应用进程状态。
- SQLite 可写状态。
- AList API 可达性。
- 夸克目录读写检查。
- 本地磁盘剩余空间。
- 上传失败率和远端孤立文件数量。
- WebSocket 当前连接数。

### 14.2 日志

- 采用结构化日志。
- 不记录 Cookie、Session、AList 凭据和私信正文。
- 应用日志默认保留 14 天。
- 限制 journald 总量约 200～300 MiB。
- 上传日志记录用户、对象 ID、大小、耗时和结果，不记录原始文件内容。

### 14.3 告警

第一版至少提供管理员后台告警：

- AList 或夸克不可访问。
- 数据库备份失败。
- 磁盘剩余空间低于阈值。
- 连续上传失败。
- 存在长时间未清理的上传任务。

## 15. 开发阶段

### 阶段 0：技术验证

目标：验证最关键、风险最高的 AList/夸克链路。

当前进度（2026-08-25）：

- [x] 建立 Go `ObjectStorage` 接口和最小 `AListStorage` 原型。
- [x] 完成流式上传、读取、Range、元数据、移动、重命名和删除的模拟集成测试。
- [x] 增加根目录隔离、路径穿越拒绝和统一错误映射。
- [x] 提供真实 AList 探针及大文件验证入口。
- [x] 在专用 AList/夸克目录完成小文件上传、下载校验、移动、重命名和删除闭环。
- [x] 在专用夸克目录完成真实 PNG 图片的上传、Range、哈希和清理验证。
- [x] 完成专用 AList 用户自动登录取 Token，避免浏览器 Token 注销后失效。
- [x] 在专用夸克目录完成小型 MP4 录屏的上传、Range、哈希和清理验证。
- [x] 在专用夸克目录完成 222.34 MiB 中型 MP4 的流式上传、Range、哈希、内存和清理验证。
- [x] 在专用夸克目录完成 2.545 GiB 4K MP4 的 Range、完整哈希、低内存和清理验证（上传命中秒传）。
- [x] 确认大文件完整下载存在夸克分块 `unexpected EOF` 与自动重试，最终数据完整但链路有间歇性停顿。
- [ ] 实际部署后复测大文件连续下载与视频播放稳定性，区分本地网络波动和夸克/AList 链路问题（非当前阻塞项）。
- [ ] 记录上传速度、内存峰值、断网行为和 Cookie 失效表现。

任务：

- 在开发环境部署 AList 并连接专用夸克目录。
- 实现最小 `AListStorage` 原型。
- 测试图片和 1～2 GiB 视频的流式上传。
- 测试读取、Range 请求、删除、重命名和 Cookie 失效。
- 验证生产机不产生完整临时文件。
- 记录实际上传速度、内存峰值和失败行为。

验收条件：

- 上传成功后的远端文件大小和哈希一致。
- 上传过程中应用内存保持在设定阈值内。
- 网络中断不会创建已发布的损坏媒体记录。
- AList 不可用时应用可以明确降级并报告错误。

### 阶段 1：基础平台

目标：建立可登录、可部署、可备份的应用骨架。

当前进度（2026-08-25）：

- [x] 正式选择 Vue 3，并初始化 Vue、Go 与 SQLite 迁移结构。
- [x] 实现首次管理员引导、邀请注册、登录、退出和持久 Session Cookie。
- [x] 实现管理员/成员权限隔离、登录设备查看与指定会话注销。
- [x] 实现个人资料 API 与响应式登录、注册、应用壳和资料界面。
- [x] 实现结构化日志、进程/数据库健康检查与基础审计记录。
- [x] 实现 SQLite 在线备份、完整性验证和拒绝覆盖式恢复工具。
- [x] 提供 systemd 服务/定时备份与 Nginx HTTPS 反代模板。
- [x] 通过竞态测试、权限集成测试、会话重启测试、备份恢复测试和前端生产构建。
- [ ] 在远程部署环境安装模板并完成一次真实重启与定时备份演练。

任务：

- 初始化 Go、前端和数据库迁移结构。
- 实现邀请注册、登录、会话和个人资料。
- 实现配置加载、日志、健康检查和审计基础。
- 建立 systemd 与 Nginx 开发部署模板。
- 实现数据库自动备份。

验收条件：

- 邀请制注册和登录流程完整。
- 权限测试通过。
- 服务重启后会话和数据正常。
- 能从备份恢复到全新数据库。

### 阶段 2：纪念册核心

目标：完成最小可用纪念册。

当前进度（2026-08-25）：

- [x] 建立帖子、媒体、标签、评论、回应、留言和上传任务的数据模型。
- [x] 实现帖子草稿、提交审核、管理员发布/隐藏、软删除和成员可见性规则。
- [x] 实现首页真实内容流、个人草稿列表、发布编辑器和管理员审核入口。
- [x] 接入 AList 流式媒体上传、任务状态、用户配额、幂等请求和失败清理。
- [x] 实现图片预览、视频播放、媒体尺寸/时长元数据和远端不可用占位。
- [x] 实现响应式照片墙与按内容日期聚合的时间线。
- [x] 实现 AList 头像上传、替换、成员可见头像与资料页头像编辑。
- [x] 实现头像方形裁剪、256/512/1024 输出、大于 2 MiB 原图的浏览器端 JPEG 压缩与预览失败回退。
- [x] 实现 2 GiB 有界本地媒体缓存、完整读取入缓存、上传预热、Range 命中和 LRU 淘汰。
- [x] 实现帖子详情、点赞切换、评论发布/列表和作者或管理员删除。
- [ ] 实现服务端视频封面提取和历史媒体元数据补全。
- [x] 实现留言册；支持宿舍公共页、成员个人页、图片/视频附件，以及作者删除、接收者隐藏和管理员管理；评论和点赞互动已完成。
- [ ] 实现成员个人主页与内容筛选。

本阶段的接口、状态机和验收切片记录在 [阶段 2：纪念册核心](./docs/PHASE_2_MEMORIAL_CORE.md)。

任务：

- 实现流式媒体上传。
- 实现帖子、草稿、审核和个人主页。
- 实现照片墙、时间线、留言和评论。
- 实现缩略图、媒体详情和远端不可用占位。
- 实现用户与总存储配额。

验收条件：

- 5～6 名测试用户可以独立投稿。
- 图文和视频内容可以稳定展示。
- 未审核内容不会被普通成员看到。
- 手机浏览器可以完成核心流程。

### 阶段 3：论坛与互动

目标：完成异步社区功能。

任务：

- 论坛板块、主题和回复。
- 点赞、提及、站内通知和未读状态。
- 搜索帖子、留言和论坛内容。
- 管理员内容管理和审计页面。

验收条件：

- 引用、回复和提及关系正确。
- 通知不会重复或遗漏。
- 搜索结果遵守内容可见权限。

### 阶段 4：群聊与私信

目标：完成实时交流能力。

任务：

- WebSocket 连接、鉴权和重连。
- 宿舍群聊和一对一私信。
- 已读游标、未读数量和消息撤回。
- 聊天媒体附件和历史分页。

验收条件：

- 多设备同时在线时消息顺序可接受。
- 断线重连不会永久丢失消息。
- 私信权限无法通过修改请求参数绕过。
- 服务重启后聊天历史完整。

### 阶段 5：归档与上线

目标：达到可长期运行和可离线保存状态。

任务：

- 完整导出与静态纪念册生成。
- 媒体校验清单与孤立文件检查。
- 安全检查、限流、日志限制和恢复演练。
- 生产机部署、监控和运维文档。
- 真实用户试用与问题修复。

验收条件：

- 可以从零恢复数据库和应用配置。
- 可以生成不依赖在线服务的静态归档。
- 生产机内存、磁盘和日志保持在预算内。
- AList 管理接口未暴露到公网。

## 16. 测试计划

### 16.1 单元测试

- 权限判断。
- 存储路径生成。
- 配额计算。
- MIME 与文件头校验。
- 会话和邀请过期。
- 消息已读游标。
- 软删除与恢复。

### 16.2 集成测试

- SQLite 迁移与备份恢复。
- AList 上传、读取、删除和错误映射。
- 上传成功但数据库失败的补偿清理。
- AList 或夸克断线后的降级行为。
- WebSocket 鉴权、重连和历史补拉。

### 16.3 端到端测试

- 邀请注册与登录。
- 发布图文和视频纪念内容。
- 审核后进入照片墙。
- 论坛发帖与回复。
- 群聊和私信。
- 用户删除、管理员恢复和最终导出。

### 16.4 性能与资源测试

- 两个 2 GiB 文件并发上传。
- 6 个用户同时保持 WebSocket 连接。
- 1 万条聊天消息分页。
- 1 万个媒体记录的照片墙分页。
- 远端存储响应缓慢时的超时和内存占用。
- 连续运行 7 天后的日志、缓存和数据库增长。

## 17. 主要风险

| 风险 | 影响 | 缓解措施 |
| --- | --- | --- |
| 夸克 Cookie 失效 | 媒体无法上传或读取 | 健康检查、管理员告警、凭据更新流程 |
| 夸克限速或接口变化 | 视频播放和上传不稳定 | AList 适配层、下载降级、后续 S3 迁移能力 |
| AList 漏洞或凭据泄露 | 网盘账号被控制 | 专用账号、回环监听、及时升级、最小权限 |
| 生产机内存不足 | 进程被杀或大量 Swap | Go 单体、有限缓冲、并发限制、systemd 限额 |
| Nginx 写入临时大文件 | 系统盘被占满 | `proxy_request_buffering off`、临时目录配额 |
| SQLite 损坏或主机丢失 | 文字和关系数据丢失 | WAL、每日备份、异机副本、恢复演练 |
| 夸克成为唯一媒体副本 | 长期纪念资料丢失 | 最终离线归档、第二存储副本、校验清单 |
| 私信隐私边界不清 | 成员信任受损 | 上线前明确政策、最小化日志和管理入口 |

## 18. 上线前必须确定的产品决策

- [x] 最终前端选择 Vue 3。
- [ ] 纪念内容是否默认需要管理员审核。
- [ ] 管理员是否具有读取私信正文的能力。
- [ ] 用户是否可以下载他人原始媒体。
- [ ] 用户是否可以创建外部公开分享链接。
- [ ] 投稿截止后的编辑和撤回规则。
- [ ] 账号注销后的内容处理规则。
- [ ] 夸克专用账号及目录准备。
- [ ] 第二媒体副本或最终离线副本方案。
- [ ] 应用域名与备份密钥保管人。

## 19. 近期行动清单

1. 创建专用测试目录，不使用真实纪念资料。
2. 在本地开发环境运行 AList 并连接夸克测试目录。
3. 完成 `AListStorage` 技术原型。
4. 使用图片和大视频测试流式上传、Range 读取及失败恢复。
5. 记录 AList 与夸克链路的性能数据。
6. 根据测试结果冻结存储接口和上传限制。
7. 初始化正式代码仓库与阶段 1 应用骨架。

## 20. 完成定义

项目达到正式完成状态时，应同时满足：

- 全部成员可以稳定使用核心纪念、论坛和通信功能。
- 原始媒体不持久存放在生产主机。
- 应用在生产主机资源预算内连续稳定运行。
- 数据库、配置和媒体清单可以备份并实际恢复。
- 夸克不可用时不会导致文字和关系数据损坏。
- 权限、私信和管理员能力有明确且可见的说明。
- 可以生成一份不依赖当前服务器的完整纪念归档。
